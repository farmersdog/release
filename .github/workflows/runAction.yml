name: Test Action
on:
  push:
    tags:
      - '*'

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: release
      - name: Test run
        uses: ./.github/actions/release
