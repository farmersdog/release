name: On Tag
on:
  push:
    tags:
      - '*'

jobs:
  prerelease:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: action
      - name: Test farmersdog/release action
        uses: './action'
